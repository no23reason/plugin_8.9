// (C) 2021 GoodData Corporation
import React, { useCallback } from "react";
import { areObjRefsEqual } from "@gooddata/sdk-model";
import { GranteeList } from "./GranteeList";
import { AddGranteeSelect } from "./AddGranteeSelect";
/**
 * @internal
 */
export var AddGranteeContent = function (props) {
    var appliedGrantees = props.appliedGrantees, currentUserRef = props.currentUserRef, addedGrantees = props.addedGrantees, onDelete = props.onDelete, onAddUserOrGroups = props.onAddUserOrGroups;
    var onSelectGrantee = useCallback(function (grantee) {
        if (!appliedGrantees.some(function (g) { return areObjRefsEqual(g.id, grantee.id); })) {
            onAddUserOrGroups(grantee);
        }
    }, [appliedGrantees, onAddUserOrGroups]);
    return (React.createElement(React.Fragment, null,
        React.createElement(AddGranteeSelect, { currentUserRef: currentUserRef, appliedGrantees: appliedGrantees, onSelectGrantee: onSelectGrantee }),
        React.createElement(GranteeList, { grantees: addedGrantees, mode: "AddGrantee", onDelete: onDelete })));
};
//# sourceMappingURL=AddGranteeContent.js.map