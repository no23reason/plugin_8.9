// (C) 2022 GoodData Corporation
import { __read } from "tslib";
import { useEffect, useState } from "react";
export function useContentHeight(element) {
    var _a = __read(useState(-1), 2), contentHeight = _a[0], setContentHeight = _a[1];
    useEffect(function () {
        var id = -1;
        function onFrame() {
            if (element && contentHeight !== element.scrollHeight) {
                setContentHeight(element.scrollHeight);
            }
            id = window.requestAnimationFrame(onFrame);
        }
        id = window.requestAnimationFrame(onFrame);
        return function () { return window.cancelAnimationFrame(id); };
    }, [element, contentHeight, setContentHeight]);
    return contentHeight;
}
//# sourceMappingURL=useContentHeight.js.map