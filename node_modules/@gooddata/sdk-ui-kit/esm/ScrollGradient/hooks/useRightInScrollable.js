// (C) 2022 GoodData Corporation
import { __read } from "tslib";
import { useState, useEffect } from "react";
import { useNumberState } from "./useNumberState";
export function useRightInScrollable() {
    var _a = __read(useNumberState(), 2), right = _a[0], setRight = _a[1];
    var _b = __read(useState(null), 2), content = _b[0], setContent = _b[1];
    // eslint-disable-next-line react-hooks/exhaustive-deps
    useEffect(function () {
        if (content) {
            var style = window.getComputedStyle(content, ":before");
            var currentRight = content.offsetWidth - parseFloat(style.width);
            if (currentRight !== right) {
                setRight(currentRight);
            }
        }
    });
    return {
        right: right,
        content: content,
        setContent: setContent,
    };
}
//# sourceMappingURL=useRightInScrollable.js.map