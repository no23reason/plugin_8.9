import { __assign } from "tslib";
import { uriRef } from "@gooddata/sdk-model";
import { convertFilterContext } from "./filterContext";
export var convertAlert = function (alert, filterContext) {
    var _a = alert.kpiAlert, _b = _a.content, dashboard = _b.dashboard, isTriggered = _b.isTriggered, kpi = _b.kpi, threshold = _b.threshold, whenTriggered = _b.whenTriggered, _c = _a.meta, uri = _c.uri, identifier = _c.identifier, title = _c.title, summary = _c.summary;
    return __assign(__assign({ title: title, description: summary }, (uri
        ? {
            ref: uriRef(uri),
            identifier: identifier,
            uri: uri,
        }
        : {})), { dashboard: uriRef(dashboard), widget: uriRef(kpi), threshold: threshold,
        whenTriggered: whenTriggered,
        isTriggered: isTriggered, filterContext: filterContext && convertFilterContext(filterContext) });
};
//# sourceMappingURL=alerts.js.map