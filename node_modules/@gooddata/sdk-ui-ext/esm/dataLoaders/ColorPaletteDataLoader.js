import { dataLoaderAbstractFactory } from "./DataLoaderAbstractFactory";
var ColorPaletteDataLoader = /** @class */ (function () {
    function ColorPaletteDataLoader(workspace) {
        this.workspace = workspace;
    }
    ColorPaletteDataLoader.prototype.getColorPalette = function (backend) {
        var _this = this;
        if (!this.cachedColorPalette) {
            this.cachedColorPalette = backend
                .workspace(this.workspace)
                .styling()
                .getColorPalette()
                .catch(function (error) {
                _this.cachedColorPalette = undefined;
                throw error;
            });
        }
        return this.cachedColorPalette;
    };
    return ColorPaletteDataLoader;
}());
/**
 * @internal
 */
export var colorPaletteDataLoaderFactory = dataLoaderAbstractFactory(function (workspace) { return new ColorPaletteDataLoader(workspace); });
//# sourceMappingURL=ColorPaletteDataLoader.js.map