import { useBackend, useCancelablePromise, } from "@gooddata/sdk-ui";
import { backendInvariant } from "../utils";
/**
 * Hook allowing to get current user details
 * @param config - configuration of the hook
 * @internal
 */
export function useCurrentUser(_a) {
    var backend = _a.backend, onCancel = _a.onCancel, onError = _a.onError, onLoading = _a.onLoading, onPending = _a.onPending, onSuccess = _a.onSuccess;
    var effectiveBackend = useBackend(backend);
    backendInvariant(effectiveBackend, "useCurrentUser");
    var promise = function () { return effectiveBackend.currentUser().getUser(); };
    return useCancelablePromise({ promise: promise, onCancel: onCancel, onError: onError, onLoading: onLoading, onPending: onPending, onSuccess: onSuccess }, [
        effectiveBackend,
    ]);
}
//# sourceMappingURL=useCurrentUser.js.map