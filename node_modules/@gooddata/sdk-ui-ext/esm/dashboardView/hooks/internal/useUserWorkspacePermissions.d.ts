import { IAnalyticalBackend, IWorkspacePermissions } from "@gooddata/sdk-backend-spi";
import { GoodDataSdkError, UseCancelablePromiseCallbacks, UseCancelablePromiseState } from "@gooddata/sdk-ui";
interface IUseUserWorkspacePermissionsConfig extends UseCancelablePromiseCallbacks<IWorkspacePermissions, GoodDataSdkError> {
    /**
     * Backend to work with.
     *
     * Note: the backend must come either from this property or from BackendContext. If you do not specify
     * backend here, then the hook MUST be called within an existing BackendContext.
     */
    backend?: IAnalyticalBackend;
    /**
     * Workspace to work with.
     *
     * Note: the workspace must come either from this property or from WorkspaceContext. If you do not specify
     * workspace here, then the hook MUST be called within an existing WorkspaceContext.
     */
    workspace?: string;
}
/**
 * Hook allowing to download user workspace permissions
 * @param config - configuration of the hook
 * @internal
 */
export declare function useUserWorkspacePermissions({ backend, onCancel, onError, onLoading, onPending, onSuccess, workspace, }: IUseUserWorkspacePermissionsConfig): UseCancelablePromiseState<IWorkspacePermissions, any>;
export {};
//# sourceMappingURL=useUserWorkspacePermissions.d.ts.map