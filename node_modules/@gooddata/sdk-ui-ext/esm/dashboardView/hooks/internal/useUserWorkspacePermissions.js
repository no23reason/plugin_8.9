import { useBackend, useCancelablePromise, useWorkspace, } from "@gooddata/sdk-ui";
import { userWorkspacePermissionsDataLoaderFactory } from "../../../dataLoaders";
import { backendInvariant, workspaceInvariant } from "../utils";
/**
 * Hook allowing to download user workspace permissions
 * @param config - configuration of the hook
 * @internal
 */
export function useUserWorkspacePermissions(_a) {
    var backend = _a.backend, onCancel = _a.onCancel, onError = _a.onError, onLoading = _a.onLoading, onPending = _a.onPending, onSuccess = _a.onSuccess, workspace = _a.workspace;
    var effectiveBackend = useBackend(backend);
    var effectiveWorkspace = useWorkspace(workspace);
    backendInvariant(effectiveBackend, "useUserWorkspacePermissions");
    workspaceInvariant(effectiveWorkspace, "useUserWorkspacePermissions");
    var loader = userWorkspacePermissionsDataLoaderFactory.forWorkspace(effectiveWorkspace);
    var promise = function () { return loader.getUserWorkspacePermissions(effectiveBackend); };
    return useCancelablePromise({ promise: promise, onCancel: onCancel, onError: onError, onLoading: onLoading, onPending: onPending, onSuccess: onSuccess }, [
        effectiveBackend,
        effectiveWorkspace,
    ]);
}
//# sourceMappingURL=useUserWorkspacePermissions.js.map