import { IAnalyticalBackend, IUser } from "@gooddata/sdk-backend-spi";
import { GoodDataSdkError, UseCancelablePromiseCallbacks, UseCancelablePromiseState } from "@gooddata/sdk-ui";
interface IUseCurrentUserConfig extends UseCancelablePromiseCallbacks<IUser, GoodDataSdkError> {
    /**
     * Backend to work with.
     *
     * Note: the backend must come either from this property or from BackendContext. If you do not specify
     * backend here, then the hook MUST be called within an existing BackendContext.
     */
    backend?: IAnalyticalBackend;
}
/**
 * Hook allowing to get current user details
 * @param config - configuration of the hook
 * @internal
 */
export declare function useCurrentUser({ backend, onCancel, onError, onLoading, onPending, onSuccess, }: IUseCurrentUserConfig): UseCancelablePromiseState<IUser, any>;
export {};
//# sourceMappingURL=useCurrentUser.d.ts.map