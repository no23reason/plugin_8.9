import { IAnalyticalBackend, IUserWorkspaceSettings } from "@gooddata/sdk-backend-spi";
import { GoodDataSdkError, UseCancelablePromiseCallbacks, UseCancelablePromiseState } from "@gooddata/sdk-ui";
interface IUseUserWorkspaceSettingsConfig extends UseCancelablePromiseCallbacks<IUserWorkspaceSettings, GoodDataSdkError> {
    /**
     * Backend to work with.
     *
     * Note: the backend must come either from this property or from BackendContext. If you do not specify
     * backend here, then the hook MUST be called within an existing BackendContext.
     */
    backend?: IAnalyticalBackend;
    /**
     * Workspace where the insight exists.
     *
     * Note: the workspace must come either from this property or from WorkspaceContext. If you do not specify
     * workspace here, then the hook MUST be called within an existing WorkspaceContext.
     */
    workspace?: string;
}
/**
 * Hook allowing to download user workspace settings
 * @param config - configuration of the hook
 * @internal
 */
export declare function useUserWorkspaceSettings({ backend, onCancel, onError, onLoading, onPending, onSuccess, workspace, }: IUseUserWorkspaceSettingsConfig): UseCancelablePromiseState<IUserWorkspaceSettings, any>;
export {};
//# sourceMappingURL=useUserWorkspaceSettings.d.ts.map