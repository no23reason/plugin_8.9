// (C) 2020-2021 GoodData Corporation
import invariant from "ts-invariant";
function ambientInvariantFactory(itemName, providerName) {
    return function (item, contextName) {
        return invariant(item, "The " + itemName + " in " + contextName + " must be defined. Either pass it as a config prop or make sure there is a " + providerName + " up the component tree.");
    };
}
export var backendInvariant = ambientInvariantFactory("backend", "BackendProvider");
export var workspaceInvariant = ambientInvariantFactory("workspace", "WorkspaceProvider");
//# sourceMappingURL=utils.js.map