import { useBackend, useCancelablePromise, useWorkspace, } from "@gooddata/sdk-ui";
import { objRefToString } from "@gooddata/sdk-model";
import { dashboardDataLoaderFactory } from "../../dataLoaders";
import { backendInvariant, workspaceInvariant } from "./utils";
/**
 * Hook allowing to download dashboard data
 * @param config - configuration of the hook
 * @beta
 * @deprecated Will be removed in the 8.10.0 release. Superseded by Dashboard component; please see `@gooddata/sdk-ui-dashboard` and GoodData.UI documentation for v8.7
 */
export function useDashboard(_a) {
    var dashboard = _a.dashboard, backend = _a.backend, onCancel = _a.onCancel, onError = _a.onError, onLoading = _a.onLoading, onPending = _a.onPending, onSuccess = _a.onSuccess, workspace = _a.workspace;
    var effectiveBackend = useBackend(backend);
    var effectiveWorkspace = useWorkspace(workspace);
    backendInvariant(effectiveBackend, "useDashboard");
    workspaceInvariant(effectiveWorkspace, "useDashboard");
    var loader = dashboardDataLoaderFactory.forWorkspace(effectiveWorkspace);
    var promise = function () { return loader.getDashboard(effectiveBackend, dashboard); };
    return useCancelablePromise({ promise: promise, onCancel: onCancel, onError: onError, onLoading: onLoading, onPending: onPending, onSuccess: onSuccess }, [
        effectiveBackend,
        effectiveWorkspace,
        objRefToString(dashboard),
    ]);
}
//# sourceMappingURL=useDashboard.js.map