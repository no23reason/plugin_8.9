// (C) 2007-2020 GoodData Corporation
import React from "react";
import cx from "classnames";
export function DashboardLayoutWidgetRenderer(props) {
    var item = props.item, screen = props.screen, debug = props.debug, className = props.className, contentRef = props.contentRef, isResizedByLayoutSizingStrategy = props.isResizedByLayoutSizingStrategy, minHeight = props.minHeight, height = props.height, allowOverflow = props.allowOverflow, children = props.children;
    var _a = item.size()[screen], heightAsRatio = _a.heightAsRatio, gridHeight = _a.gridHeight;
    var style = React.useMemo(function () {
        var computedStyle = {
            minHeight: minHeight,
            height: height,
        };
        if (allowOverflow) {
            computedStyle.overflowX = "hidden";
            computedStyle.overflowY = "auto";
        }
        if (debug) {
            if (!heightAsRatio) {
                computedStyle.outline = "solid 1px yellow";
            }
            else {
                computedStyle.border = isResizedByLayoutSizingStrategy
                    ? "dashed 1px #d6d6d6"
                    : "solid 1px green";
            }
        }
        return computedStyle;
    }, [heightAsRatio, debug, screen, height, minHeight, isResizedByLayoutSizingStrategy]);
    var getClassNames = function () {
        return cx("gd-fluidlayout-column-container", className, {
            "custom-height": !!gridHeight,
        });
    };
    return (React.createElement("div", { ref: contentRef, className: getClassNames(), style: style }, children));
}
//# sourceMappingURL=DashboardLayoutWidgetRenderer.js.map