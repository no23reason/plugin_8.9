import { __assign } from "tslib";
import { BucketNames } from "@gooddata/sdk-ui";
import { getDateItems, findBucket, limitNumberOfMeasuresInBuckets, getAllMeasures, } from "../../../utils/bucketHelper";
export var getXirrBuckets = function (_a) {
    var buckets = _a.buckets;
    var limitedMeasureBuckets = limitNumberOfMeasuresInBuckets(buckets, 1);
    var currentMeasureBucket = findBucket(limitedMeasureBuckets, BucketNames.MEASURES);
    var currentAttributeBucket = findBucket(buckets, BucketNames.ATTRIBUTE);
    var measureItem = getAllMeasures(limitedMeasureBuckets)[0];
    var dateAttributeItem = getDateItems(buckets)[0];
    return [
        __assign(__assign({}, currentMeasureBucket), { localIdentifier: BucketNames.MEASURES, items: measureItem ? [measureItem] : [] }),
        __assign(__assign({}, currentAttributeBucket), { localIdentifier: BucketNames.ATTRIBUTE, items: dateAttributeItem ? [dateAttributeItem] : [] }),
    ];
};
//# sourceMappingURL=xirrBucketHelper.js.map