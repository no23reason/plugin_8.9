// (C) 2019-2020 GoodData Corporation
import { unmountComponentAtNode } from "react-dom";
export function unmountComponentsAtNodes(elementSelectors, _a) {
    if (elementSelectors === void 0) { elementSelectors = []; }
    var _b = _a === void 0 ? {
        unmount: unmountComponentAtNode,
        documentInstance: document,
    } : _a, unmount = _b.unmount, documentInstance = _b.documentInstance;
    elementSelectors.forEach(function (elementSelector) {
        var element = documentInstance.querySelector(elementSelector);
        if (element) {
            unmount(element);
        }
    });
}
//# sourceMappingURL=domHelper.js.map