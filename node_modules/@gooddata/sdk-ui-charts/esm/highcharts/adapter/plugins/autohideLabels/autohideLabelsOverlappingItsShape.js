// (C) 2007-2020 GoodData Corporation
import { getDataPoints, getVisibleSeries } from "../../../chartTypes/_chartCreators/helpers";
import { intersectsParentLabel, isLabelOverlappingItsShape, hideDataLabel, showDataLabel, } from "../../../chartTypes/_chartCreators/dataLabelsHelpers";
function autohideLabelsOverlappingItsShape(
// eslint-disable-next-line @typescript-eslint/explicit-module-boundary-types
chart, hideFunction, showFunction) {
    if (hideFunction === void 0) { hideFunction = hideDataLabel; }
    if (showFunction === void 0) { showFunction = showDataLabel; }
    var visibleSeries = getVisibleSeries(chart);
    var visiblePoints = getDataPoints(visibleSeries);
    visiblePoints.forEach(function (point) {
        if (point) {
            if (isLabelOverlappingItsShape(point) || intersectsParentLabel(point, visiblePoints)) {
                hideFunction(point);
            }
            else {
                showFunction(point);
            }
        }
    });
}
export default autohideLabelsOverlappingItsShape;
//# sourceMappingURL=autohideLabelsOverlappingItsShape.js.map