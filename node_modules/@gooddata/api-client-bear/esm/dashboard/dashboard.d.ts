import { GdcExport, GdcFilterContext } from "@gooddata/api-model-bear";
import { XhrModule } from "../xhr";
import { IPollingOptions } from "../util";
export declare class DashboardModule {
    private xhr;
    constructor(xhr: XhrModule);
    exportToPdf(projectId: string, dashboardUri: string, filters?: GdcFilterContext.FilterContextItem[], pollingOptions?: IPollingOptions): Promise<GdcExport.IExportResponse>;
    private pollPdfFile;
    private getDashboardExportPayload;
}
