import isEmpty from "lodash/isEmpty";
/**
 * @public
 */
export var GdcKpi;
(function (GdcKpi) {
    function isKpiContentWithoutComparison(obj) {
        return !isEmpty(obj) && obj.comparisonType === "none";
    }
    GdcKpi.isKpiContentWithoutComparison = isKpiContentWithoutComparison;
    function isKpi(obj) {
        return !isEmpty(obj) && obj.meta.category === "kpi";
    }
    GdcKpi.isKpi = isKpi;
    function isWrappedKpi(obj) {
        return !isEmpty(obj) && !!obj.kpi;
    }
    GdcKpi.isWrappedKpi = isWrappedKpi;
})(GdcKpi || (GdcKpi = {}));
//# sourceMappingURL=GdcKpi.js.map