// (C) 2020-2021 GoodData Corporation
import React from "react";
import cx from "classnames";
import { Bubble, BubbleHoverTrigger, Item } from "@gooddata/sdk-ui-kit";
var tooltipAlignPoints = [{ align: "cl cr" }];
export var DashboardInsightMenuItemButton = function (props) {
    var itemId = props.itemId, itemName = props.itemName, disabled = props.disabled, icon = props.icon, onClick = props.onClick, tooltip = props.tooltip, className = props.className;
    var contentComponent = (
    // for JSX icons we need an extra gd-icon-wrapper class to align the icon and the text vertically
    React.createElement("span", { className: cx({ "gd-icon-wrapper": icon && typeof icon !== "string" }) },
        icon ? typeof icon === "string" ? React.createElement("i", { className: icon }) : icon : null,
        itemName));
    if (disabled) {
        var button = (React.createElement(Item, { className: className, disabled: disabled }, contentComponent));
        if (tooltip) {
            return (React.createElement(BubbleHoverTrigger, { className: "s-gd-bubble-trigger-options-menu-item" },
                button,
                React.createElement(Bubble, { className: "bubble-primary bubble-primary-" + itemId + " s-bubble-primary-" + itemId, alignPoints: tooltipAlignPoints }, tooltip)));
        }
        else {
            return button;
        }
    }
    else {
        return (React.createElement(Item, { className: className, onClick: onClick, disabled: disabled }, contentComponent));
    }
};
//# sourceMappingURL=DashboardInsightMenuItemButton.js.map