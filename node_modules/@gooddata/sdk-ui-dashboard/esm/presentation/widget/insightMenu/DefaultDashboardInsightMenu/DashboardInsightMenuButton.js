// (C) 2021 GoodData Corporation
import React, { useCallback } from "react";
import cx from "classnames";
import { stringUtils } from "@gooddata/util";
import { objRefToString } from "@gooddata/sdk-model";
import { widgetRef } from "@gooddata/sdk-backend-spi";
export var DashboardInsightMenuButton = function (props) {
    var isOpen = props.isOpen, items = props.items, widget = props.widget, onClick = props.onClick;
    var onMenuButtonClick = useCallback(function (e) {
        e.stopPropagation();
        onClick();
    }, [onClick]);
    if (!items.length) {
        return null;
    }
    var widgetRefAsString = objRefToString(widgetRef(widget));
    var optionsIconClasses = cx("dash-item-action-options", "dash-item-action-widget-options", "s-dash-item-action-widget-options", "dash-item-action-widget-options-" + stringUtils.simplifyText(widgetRefAsString), "s-dash-item-action-widget-options-" + stringUtils.simplifyText(widgetRefAsString), {
        "dash-item-action-widget-options-active": isOpen,
    });
    return (React.createElement("div", { className: "dash-item-action-placeholder s-dash-item-action-placeholder", onClick: onMenuButtonClick },
        React.createElement("div", { className: optionsIconClasses })));
};
//# sourceMappingURL=DashboardInsightMenuButton.js.map