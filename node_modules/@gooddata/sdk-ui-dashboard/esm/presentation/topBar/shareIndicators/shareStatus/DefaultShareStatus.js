import { __assign } from "tslib";
// (C) 2021 GoodData Corporation
import React from "react";
import { selectBackendCapabilities, selectCanManageAnalyticalDashboard, selectSettings, useDashboardSelector, } from "../../../../model";
import { ShareStatusIndicator } from "./ShareStatusIndicator";
/**
 * @alpha
 */
export var DefaultShareStatus = function (props) {
    var settings = useDashboardSelector(selectSettings);
    var capabilities = useDashboardSelector(selectBackendCapabilities);
    var canManageAnalyticalDashboard = useDashboardSelector(selectCanManageAnalyticalDashboard);
    if (!settings.enableAnalyticalDashboardPermissions ||
        !capabilities.supportsAccessControl ||
        !canManageAnalyticalDashboard) {
        return null;
    }
    return React.createElement(ShareStatusIndicator, __assign({}, props));
};
//# sourceMappingURL=DefaultShareStatus.js.map