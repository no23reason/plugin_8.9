import { ObjRefMap } from "../metadata/objRefMap";
/**
 * Creates an {@link ObjRefMap} mapping date attribute ObjRef to an entry that contains the date attribute and the date dataset to which it belongs
 *
 * @param items - items to add to mapping
 * @param strictTypeCheck - whether to do strict type checking in idRefs
 * @internal
 */
export function newCatalogDateAttributeWithDatasetMap(items, strictTypeCheck) {
    if (strictTypeCheck === void 0) { strictTypeCheck = false; }
    var config = {
        type: "attribute",
        strictTypeCheck: strictTypeCheck,
        idExtract: function (i) { return i.attribute.attribute.id; },
        uriExtract: function (i) { return i.attribute.attribute.uri; },
        refExtract: function (i) { return i.attribute.attribute.ref; },
    };
    var map = new ObjRefMap(config);
    return map.fromItems(items);
}
//# sourceMappingURL=dateAttributeWithDatasetMap.js.map