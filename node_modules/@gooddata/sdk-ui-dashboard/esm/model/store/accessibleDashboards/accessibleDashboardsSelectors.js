// (C) 2021 GoodData Corporation
import { createSelector } from "@reduxjs/toolkit";
import { newMapForObjectWithIdentity } from "../../../_staging/metadata/objRefMap";
import { accessibleDashboardsEntityAdapter } from "./accessibleDashboardsEntityAdapter";
var selectSelf = createSelector(function (state) { return state; }, function (state) { return state.accessibleDashboards; });
var adapterSelectors = accessibleDashboardsEntityAdapter.getSelectors(selectSelf);
/**
 * Select all accessible dashboard in project.
 *
 * @alpha
 */
export var selectAccessibleDashboards = adapterSelectors.selectAll;
/**
 * Select all accessible dashboard in project and returns them in a mapping of obj ref to the insight object.
 *
 * @alpha
 */
export var selectAccessibleDashboardsMap = createSelector(selectAccessibleDashboards, function (dashboards) {
    return newMapForObjectWithIdentity(dashboards);
});
//# sourceMappingURL=accessibleDashboardsSelectors.js.map