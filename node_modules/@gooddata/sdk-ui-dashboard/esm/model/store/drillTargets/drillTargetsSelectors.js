// (C) 2021 GoodData Corporation
import { drillTargetsAdapter } from "./drillTargetsEntityAdapter";
import memoize from "lodash/memoize";
import { createSelector } from "@reduxjs/toolkit";
import { serializeObjRef } from "@gooddata/sdk-model";
import { newMapForObjectWithIdentity } from "../../../_staging/metadata/objRefMap";
var entitySelectors = drillTargetsAdapter.getSelectors(function (state) { return state.drillTargets; });
var selectDrillTargetsInternal = entitySelectors.selectAll;
/**
 * Return all widgets drill targets
 * @alpha
 */
export var selectDrillTargets = createSelector(selectDrillTargetsInternal, function (drillTargets) {
    return newMapForObjectWithIdentity(drillTargets);
});
/**
 * Selects drill targets by widget ref.
 *
 * @alpha
 */
export var selectDrillTargetsByWidgetRef = memoize(function (ref) {
    return createSelector(selectDrillTargets, function (drillTargets) {
        return drillTargets.get(ref);
    });
}, serializeObjRef);
//# sourceMappingURL=drillTargetsSelectors.js.map