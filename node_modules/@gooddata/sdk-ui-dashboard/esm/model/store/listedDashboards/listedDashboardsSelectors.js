// (C) 2021 GoodData Corporation
import { createSelector } from "@reduxjs/toolkit";
import { listedDashboardsEntityAdapter } from "./listedDashboardsEntityAdapter";
import { newMapForObjectWithIdentity } from "../../../_staging/metadata/objRefMap";
var selectSelf = createSelector(function (state) { return state; }, function (state) { return state.listedDashboards; });
var adapterSelectors = listedDashboardsEntityAdapter.getSelectors(selectSelf);
/**
 * Select all listed dashboard in project.
 *
 * @alpha
 */
export var selectListedDashboards = adapterSelectors.selectAll;
/**
 * Select all listed dashboard in project and returns them in a mapping of obj ref to the insight object.
 *
 * @alpha
 */
export var selectListedDashboardsMap = createSelector(selectListedDashboards, function (dashboards) {
    return newMapForObjectWithIdentity(dashboards);
});
//# sourceMappingURL=listedDashboardsSelectors.js.map