// (C) 2021 GoodData Corporation
import { __assign } from "tslib";
import { EmptyDashboardDescriptor } from "./metaState";
import invariant from "ts-invariant";
var setMeta = function (state, action) {
    var dashboard = action.payload.dashboard;
    state.persistedDashboard = dashboard;
    state.descriptor = dashboard
        ? {
            title: dashboard.title,
            description: dashboard.description,
            tags: dashboard.tags,
            shareStatus: dashboard.shareStatus,
            isUnderStrictControl: dashboard.isUnderStrictControl,
            isLocked: dashboard.isLocked,
        }
        : __assign({}, EmptyDashboardDescriptor);
};
var setDashboardTitle = function (state, action) {
    invariant(state.descriptor);
    state.descriptor.title = action.payload;
};
export var metaReducers = {
    setMeta: setMeta,
    setDashboardTitle: setDashboardTitle,
};
//# sourceMappingURL=metaReducers.js.map