import keys from "lodash/keys";
import includes from "lodash/includes";
var RequiredConfigKeys = [
    "dateFilterConfig",
    "locale",
    "separators",
    "colorPalette",
    "settings",
];
/**
 * Tests whether the provided config is fully resolved - it contains all the necessary values.
 *
 * @param config - config to test
 */
export function isResolvedConfig(config) {
    if (!config) {
        return false;
    }
    var specifiedConfig = keys(config);
    return RequiredConfigKeys.every(function (key) { return includes(specifiedConfig, key); });
}
//# sourceMappingURL=commonTypes.js.map