// (C) 2020-2021 GoodData Corporation
import { useCallback } from "react";
import invariant from "ts-invariant";
import { isDashboardEventOrCustomDashboardEvent, } from "../events";
import { useDashboardDispatch } from "./DashboardStoreProvider";
import { triggerEvent } from "../commands";
/**
 * Convenience hook for dispatching Dashboard events.
 *
 * @returns function that you can use to dispatch Dashboard events
 * @alpha
 */
export var useDashboardEventDispatch = function () {
    var dispatch = useDashboardDispatch();
    return useCallback(function (eventBody) {
        invariant(isDashboardEventOrCustomDashboardEvent(eventBody), "Unsupported event passed to useDashboardEventDispatch result.");
        var command = triggerEvent(eventBody);
        dispatch(command);
    }, [dispatch]);
};
//# sourceMappingURL=useDashboardEventDispatch.js.map