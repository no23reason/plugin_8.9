import { __read, __spread } from "tslib";
import { objRefToString } from "@gooddata/sdk-model";
import { useBackendStrict, useWorkspaceStrict, useCancelablePromise, } from "../base";
/**
 * @internal
 */
export function useInsight(config, deps) {
    var insight = config.insight;
    var backend = useBackendStrict(config.backend, "useInsight");
    var workspace = useWorkspaceStrict(config.workspace, "useInsight");
    var promise = insight ? function () { return backend.workspace(workspace).insights().getInsight(insight); } : null;
    return useCancelablePromise({ promise: promise }, __spread([
        backend,
        workspace,
        insight ? objRefToString(insight) : "__insightRef__"
    ], (deps !== null && deps !== void 0 ? deps : [])));
}
//# sourceMappingURL=useInsight.js.map