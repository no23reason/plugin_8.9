import { __extends } from "tslib";
// (C) 2007-2019 GoodData Corporation
import React from "react";
import LegendItem from "./LegendItem";
import { LegendAxisIndicator } from "./LegendAxisIndicator";
import { LEGEND_AXIS_INDICATOR, LEGEND_SEPARATOR } from "./helpers";
export var LegendSeparator = function () { return React.createElement("div", { className: "legend-separator" }); };
var LegendList = /** @class */ (function (_super) {
    __extends(LegendList, _super);
    function LegendList() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    LegendList.prototype.render = function () {
        var _a = this.props, series = _a.series, enableBorderRadius = _a.enableBorderRadius, onItemClick = _a.onItemClick, width = _a.width;
        return series.map(function (item, index) {
            var type = item.type, labelKey = item.labelKey, data = item.data;
            var borderRadius = shouldItemHaveBorderRadius(item, enableBorderRadius);
            if (type === LEGEND_AXIS_INDICATOR) {
                return React.createElement(LegendAxisIndicator, { key: index, labelKey: labelKey, data: data, width: width });
            }
            else if (type === LEGEND_SEPARATOR) {
                return React.createElement(LegendSeparator, { key: index });
            }
            else {
                return (React.createElement(LegendItem, { enableBorderRadius: borderRadius, key: index, item: item, width: width, onItemClick: onItemClick }));
            }
        });
    };
    return LegendList;
}(React.PureComponent));
export { LegendList };
function shouldItemHaveBorderRadius(item, enableBorderRadius) {
    if (enableBorderRadius === void 0) { enableBorderRadius = false; }
    if (typeof enableBorderRadius === "function") {
        return enableBorderRadius(item);
    }
    return enableBorderRadius;
}
//# sourceMappingURL=LegendList.js.map