// (C) 2007-2021 GoodData Corporation
import { getIntersectionAttributes, } from "@gooddata/sdk-ui";
export function getAvailableDrillTargets(dv) {
    var measureDescriptors = dv
        .meta()
        .measureDescriptors()
        .map(function (measure) { return ({
        measure: measure,
        attributes: dv.meta().attributeDescriptors(),
    }); });
    var rowAttributeItems = dv
        .meta()
        .attributeDescriptorsForDim(0)
        .map(function (attribute, _index, attributes) { return ({
        attribute: attribute,
        intersectionAttributes: getIntersectionAttributes(attribute, attributes),
    }); });
    return {
        measures: measureDescriptors,
        attributes: rowAttributeItems,
    };
}
//# sourceMappingURL=drillTargets.js.map