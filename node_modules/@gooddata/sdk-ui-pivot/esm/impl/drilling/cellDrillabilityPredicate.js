// (C) 2021 GoodData Corporation
import { isSomeHeaderPredicateMatched, } from "@gooddata/sdk-ui";
import { createDrillHeaders } from "./colDrillHeadersFactory";
export function isCellDrillable(colDescriptor, row, dv, drillablePredicates) {
    if (drillablePredicates.length === 0) {
        return false;
    }
    var headers = createDrillHeaders(colDescriptor, row);
    return headers.some(function (drillItem) {
        return isSomeHeaderPredicateMatched(drillablePredicates, drillItem, dv);
    });
}
//# sourceMappingURL=cellDrillabilityPredicate.js.map