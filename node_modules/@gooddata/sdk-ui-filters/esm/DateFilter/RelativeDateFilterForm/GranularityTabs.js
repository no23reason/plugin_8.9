// (C) 2019 GoodData Corporation
import React from "react";
import { FormattedMessage } from "react-intl";
import sortBy from "lodash/sortBy";
import { TabsWrapper, Tab } from "../Tabs/Tabs";
import { granularityIntlCodes } from "../constants/i18n";
var granularityOrders = {
    "GDC.time.date": 0,
    "GDC.time.week_us": 1,
    "GDC.time.month": 2,
    "GDC.time.quarter": 3,
    "GDC.time.year": 4,
};
var sortGranularities = function (granularities) {
    return sortBy(granularities, function (granularity) { return granularityOrders[granularity]; });
};
export var GranularityTabs = function (_a) {
    var availableGranularities = _a.availableGranularities, onSelectedGranularityChange = _a.onSelectedGranularityChange, selectedGranularity = _a.selectedGranularity;
    return (React.createElement(TabsWrapper, { className: "gd-relative-filter-form-granularity-tabs s-relative-filter-form-granularity-tabs" }, sortGranularities(availableGranularities).map(function (granularity) {
        var intlGranularity = granularityIntlCodes[granularity];
        return (React.createElement(Tab, { key: granularity, selected: granularity === selectedGranularity, onClick: function () { return onSelectedGranularityChange(granularity); }, className: "s-granularity-" + intlGranularity },
            React.createElement(FormattedMessage, { id: "filters.granularity." + intlGranularity + "s" })));
    })));
};
//# sourceMappingURL=GranularityTabs.js.map