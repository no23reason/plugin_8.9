// (C) 2022 GoodData Corporation
import { useEffect, useMemo } from "react";
/**
 * Resolves errors for {@link AttributeFilterButton} component.
 */
export var useOnErrorCallback = function (onError, errors) {
    var error = useMemo(function () { return errors.find(function (error) { return !!error; }); }, [errors]);
    return useEffect(function () {
        if (onError && error) {
            onError(error);
        }
    }, errors);
};
//# sourceMappingURL=useOnErrorCallback.js.map