import { DateAttributeGranularity } from "@gooddata/sdk-model";
import { DateFormatter } from "./types";
/**
 * Creates a function that takes a string date attribute value and a granularity and returns a formatted date string.
 * @param dateFormatter - function to use to format Date values to a string
 * @public
 */
export declare function createDateValueFormatter(dateFormatter: DateFormatter): (value: string, granularity: DateAttributeGranularity) => string;
//# sourceMappingURL=dateValueFormatter.d.ts.map