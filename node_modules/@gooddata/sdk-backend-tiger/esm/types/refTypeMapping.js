// (C) 2019-2021 GoodData Corporation
import invert from "lodash/invert";
import isEmpty from "lodash/isEmpty";
import values from "lodash/values";
export var tigerIdTypeToObjectType = {
    attribute: "attribute",
    metric: "measure",
    label: "displayForm",
    dataset: "dataSet",
    fact: "fact",
    variable: "variable",
    analyticalDashboard: "analyticalDashboard",
    visualizationObject: "insight",
    filterContext: "filterContext",
    dashboardPlugin: "dashboardPlugin",
};
export var objectTypeToTigerIdType = invert(tigerIdTypeToObjectType);
var validTigerTypes = Object.keys(tigerIdTypeToObjectType);
var validCompatibleTypes = values(tigerIdTypeToObjectType);
export var isTigerCompatibleType = function (obj) {
    return !isEmpty(obj) && validCompatibleTypes.some(function (type) { return type === obj; });
};
export var isTigerType = function (obj) {
    return !isEmpty(obj) && validTigerTypes.some(function (type) { return type === obj; });
};
//# sourceMappingURL=refTypeMapping.js.map